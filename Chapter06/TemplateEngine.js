// EJS / PUG -> 템플릿 엔진 html을 코드로 생성가능하게 해주는 엔진

/**==================================================
            @app.set('view engine','pug')
               뷰 템플릿 엔진을 pug로 설정

    @app.set('views',path.join(__dirname,'views'))
                뷰파일들의 경로를 설정
 =====================================================*/

// pug 파일은 들여쓰기로 부모 / 자식 태그를 구분